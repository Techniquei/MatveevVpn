name: Build

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Validate
        run: ./Scripts/validate.sh
      - name: Build DMG
        run: ./Scripts/build-dmg.sh
      - uses: actions/upload-artifact@v4
        with:
          name: matveevVpn-arm64
          path: dist/*.dmg
          if-no-files-found: error
